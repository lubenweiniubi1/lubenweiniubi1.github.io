# 周三字节OD面试备战计划

> 当前日期：2026-06-29（周一），面试日期：2026-07-01（周三）
> 可用时间：周一全天 + 周二全天 + 周三上午 = 约 2.5 天

---

## 一、面试流程预判

| 环节 | 内容 | 时长 |
|------|------|------|
| 一面（AI面） | 简历介绍、AI工具项目经验、MCP提效 | ~30min |
| 二面（技术面-1） | 简历深挖、React技术栈深度、场景题 | ~60min |
| 三面（技术面-2） | 算法/手写题、软素质 | ~60min |

---

## 二、每日时间表

### 周一（6/29）—— 项目复盘 + AI经验整理

| 时段 | 任务 | 产出 |
|------|------|------|
| 09:00-10:30 | 用 STAR 法则写出 4 段核心项目经历的口述稿 | 口述稿 |
| 10:30-12:00 | 整理 AI 辅助开发经验体系（Agent Skill、MCP、Vibecoding 工作流） | AI经验话术 |
| 13:00-15:00 | 复盘保时捷项目技术细节：Taro跨端迁移、Monorepo组件库、四端架构 | 技术QA清单 |
| 15:00-17:00 | 复盘 REA 项目技术细节：Next.js全栈、DDD、CI/CD流水线 | 技术QA清单 |
| 19:00-21:00 | React 深度知识梳理（见下方清单） | 知识脑图 |
| 21:00-22:00 | 手写题：事件循环 & 异步（3道） | 代码 |

### 周二（6/30）—— 技术深度 + 手写题突击

| 时段 | 任务 | 产出 |
|------|------|------|
| 09:00-11:00 | React 原理深度：Fiber、Hooks、并发模式、性能优化 | 知识脑图 |
| 11:00-12:00 | 手写题：闭包 & 作用域（2道） | 代码 |
| 13:00-15:00 | 手写题：最大并发请求 + 并发请求控制器 | 代码 |
| 15:00-16:00 | 手写题：EventEmitter | 代码 |
| 16:00-17:00 | 手写题：useQuery / useDebounceFn | 代码 |
| 19:00-20:30 | 软素质准备：问题解决、沟通、最近学到的技术 | 话术 |
| 20:30-22:00 | 模拟面试：完整走一遍自我介绍 + 项目介绍 + 高频追问 | 录音自检 |

### 周三（7/1）—— 临阵磨枪

| 时段 | 任务 |
|------|------|
| 08:00-09:30 | 过一遍手写题核心代码 |
| 09:30-10:30 | 复述一遍自我介绍的3分钟版本 + AI经验2分钟版本 |
| 10:30-11:00 | 休息，放空 |
| 11:00 | 面试（预估时间） |

---

## 三、必学知识点清单

### 3.1 简历项目口述（STAR 法则）

对以下 4 段经历，每一段用 STAR 写 2-3 分钟口述稿：

1. **保时捷微信→抖音小程序迁移**（体现跨端架构能力）
2. **保时捷跨端组件库建设**（体现组件化/工程化能力）
3. **REA Group AI 工程化实践**（体现 AI 提效能力，重点！）
4. **乡村振兴 IoT 项目**（体现独立交付能力）

### 3.2 AI 辅助开发经验（重点准备！面试明确提到）

用以下框架组织你的 AI 经验话术：

```
1. 背景：REA Group 项目面临的效率瓶颈
2. 方案：基于 Claude Code 封装自定义 Agent Skill
3. 落地：
   - Spec Skill：需求 → 结构化Spec
   - Unit Test Skill：自动生成测试用例
   - Code Review Skill：提交前预检
   - Issue Skill：根因分析报告
4. 效果：流水线 → 需求拆解→开发→单测→Review→CI/CD
5. 延伸：MCP 协议的理解、如何集成外部工具
```

**面试可能追问：**
- MCP 是什么？和传统 API 集成的区别？
- 你怎么评估 AI 生成代码的质量？
- 使用 AI 工具后，团队流程有什么变化？
- 你对 Vibecoding 的理解？

### 3.3 React 技术栈深度

| 知识点 | 必须能讲清楚 |
|--------|-------------|
| **React 渲染机制** | Virtual DOM、Reconciliation、Fiber 架构、双缓存 |
| **Hooks 原理** | useState/useEffect/useMemo/useCallback 的实现原理、闭包陷阱 |
| **状态管理** | Redux Toolkit vs MobX 选型理由、中间件原理 |
| **Next.js** | SSR/SSG/ISR 区别、App Router、Server Components |
| **性能优化** | React.memo、useMemo、useCallback、懒加载、代码分割 |
| **TaroJS** | 跨端原理、运行时 vs 编译时、平台适配机制 |
| **Monorepo** | Lerna + Yarn Workspace 的协作模式 |

### 3.4 软素质

| 问题类型 | 准备方向 |
|----------|----------|
| 进入新项目，架构不熟悉怎么办？ | 读代码 → 画架构图 → 找关键路径 → 小任务验证理解 |
| 遇到不知道怎么解决的问题？ | 拆解问题 → 搜索/问AI → 最小实验验证 → 记录沉淀 |
| 最近学到的有意思的技术？ | 准备 1-2 个（如 MCP 协议、Claude Code Agent Skill、RSC 等） |
| 沟通能力 | 准备一个跨团队/跨国协作的具体案例 |

---

## 四、手写题代表题目

### 4.1 事件循环和异步（必考范围）

**题目1：输出顺序题**
```js
// 高频考题：说出以下代码的输出顺序并解释
console.log(1);
setTimeout(() => console.log(2), 0);
new Promise((resolve) => {
  console.log(3);
  resolve();
}).then(() => console.log(4));
console.log(5);
// 答案：1 3 5 4 2
```

**题目2：async/await 与微任务**
```js
async function async1() {
  console.log('async1 start');
  await async2();
  console.log('async1 end');
}
async function async2() {
  console.log('async2');
}
console.log('script start');
setTimeout(() => console.log('setTimeout'), 0);
async1();
new Promise((resolve) => {
  console.log('promise1');
  resolve();
}).then(() => console.log('promise2'));
console.log('script end');
```

**题目3：Node.js 事件循环阶段**
- 准备讲清楚：timers → pending callbacks → idle/prepare → poll → check → close callbacks
- process.nextTick vs setImmediate 区别

### 4.2 最大并发请求 / 并发请求控制器（高频！）

这是字节面试极高概率出现的手写题，必须能手写出来。

```js
// 核心题目：实现一个并发请求控制器
class RequestPool {
  constructor(maxConcurrency) {
    this.maxConcurrency = maxConcurrency;
    this.running = 0;
    this.queue = [];
  }

  add(requestFn) {
    return new Promise((resolve, reject) => {
      const task = async () => {
        this.running++;
        try {
          const result = await requestFn();
          resolve(result);
        } catch (err) {
          reject(err);
        } finally {
          this.running--;
          this.next();
        }
      };
      if (this.running < this.maxConcurrency) {
        task();
      } else {
        this.queue.push(task);
      }
    });
  }

  next() {
    if (this.queue.length > 0 && this.running < this.maxConcurrency) {
      const task = this.queue.shift();
      task();
    }
  }
}

// 使用示例
const pool = new RequestPool(3);
const urls = ['url1', 'url2', 'url3', 'url4', 'url5'];
Promise.all(urls.map(url => pool.add(() => fetch(url)))).then(console.log);
```

**变体题：实现 Promise.all 带并发限制**
```js
async function promiseAllWithLimit(tasks, limit) {
  const results = [];
  const executing = [];
  for (const [i, task] of tasks.entries()) {
    const p = Promise.resolve().then(() => task()).then(r => { results[i] = r; });
    results.push(undefined);
    const e = p.then(() => executing.splice(executing.indexOf(e), 1));
    executing.push(e);
    if (executing.length >= limit) {
      await Promise.race(executing);
    }
  }
  await Promise.all(executing);
  return results;
}
```

### 4.3 闭包与作用域

**题目1：经典闭包陷阱**
```js
for (var i = 1; i <= 5; i++) {
  setTimeout(() => console.log(i), i * 1000);
}
// 输出什么？为什么？如何修复？（let / IIFE / setTimeout第三个参数）
```

**题目2：闭包实现私有变量**
```js
function createCounter() {
  let count = 0;
  return {
    increment: () => ++count,
    decrement: () => --count,
    getCount: () => count,
  };
}
```

**题目3：作用域链**
```js
var a = 10;
function foo() {
  console.log(a);
}
function bar() {
  var a = 20;
  foo();
}
bar(); // 输出 10（词法作用域，不是动态作用域）
```

### 4.4 EventEmitter

```js
class EventEmitter {
  constructor() {
    this.events = {};
  }

  on(event, listener) {
    if (!this.events[event]) this.events[event] = [];
    this.events[event].push(listener);
    return this;
  }

  off(event, listener) {
    if (!this.events[event]) return this;
    this.events[event] = this.events[event].filter(l => l !== listener);
    return this;
  }

  once(event, listener) {
    const wrapper = (...args) => {
      listener(...args);
      this.off(event, wrapper);
    };
    this.on(event, wrapper);
    return this;
  }

  emit(event, ...args) {
    if (!this.events[event]) return this;
    this.events[event].forEach(listener => listener(...args));
    return this;
  }
}
```

### 4.5 useQuery（自定义 Hook）

```jsx
function useQuery(url) {
  const [data, setData] = useState(null);
  const [loading, setLoading] = useState(true);
  const [error, setError] = useState(null);

  const fetchData = useCallback(async () => {
    setLoading(true);
    try {
      const res = await fetch(url);
      const json = await res.json();
      setData(json);
      setError(null);
    } catch (err) {
      setError(err);
    } finally {
      setLoading(false);
    }
  }, [url]);

  useEffect(() => {
    fetchData();
  }, [fetchData]);

  return { data, loading, error, refetch: fetchData };
}
```

### 4.6 useDebounceFn（自定义 Hook）

```jsx
function useDebounceFn(fn, delay) {
  const timerRef = useRef(null);

  const debouncedFn = useCallback((...args) => {
    if (timerRef.current) clearTimeout(timerRef.current);
    timerRef.current = setTimeout(() => {
      fn(...args);
    }, delay);
  }, [fn, delay]);

  useEffect(() => {
    return () => {
      if (timerRef.current) clearTimeout(timerRef.current);
    };
  }, []);

  return debouncedFn;
}

// 另一个常见考法：useDebounce（对值去抖）
function useDebounce(value, delay) {
  const [debouncedValue, setDebouncedValue] = useState(value);
  useEffect(() => {
    const timer = setTimeout(() => setDebouncedValue(value), delay);
    return () => clearTimeout(timer);
  }, [value, delay]);
  return debouncedValue;
}
```

---

## 五、面试话术速记卡

### 5.1 自我介绍（3分钟版）

```
我是潘林峰，7年前端开发经验，上一份工作在 Thoughtworks 做了近5年。

技术栈上，React + TypeScript + Next.js 是主力，具备全栈能力（Node.js/Express + Drizzle ORM + Docker）。
跨端方向有丰富的 TaroJS 多端实践经验。

在保时捷项目做了3年，是前端 Lead，主导了微信小程序到抖音小程序的跨端迁移，
代码复用率达到70%。同时搭建了跨端组件库，四端复用。

在 REA Group 项目做全栈，和澳洲团队全英文协作。
重点是用 Claude Code 封装了自定义 Agent Skill，把 AI 做进了完整的开发流水线：
从需求拆解到开发到测试到Review，全链路 AI 辅助。这是我最兴奋的技术实践方向。

英文可以作为工作语言，随时可以到岗。
```

### 5.2 AI 经验话术（2分钟版）

```
在 REA Group 项目中，我基于 Claude Code 设计了一套 Agent Skill 体系。

MCP 协议允许 AI 模型在 runtime 发现和调用外部工具。
我利用这个机制封装了四个 Skill：
- Spec Skill 把需求拆成结构化可执行的 Spec
- Unit Test Skill 自动生成覆盖核心逻辑和边界场景的 Jest 测试
- Code Review Skill 在提交前做自动化预检
- Issue Skill 输出结构化的根因分析报告

这套流水线的效果是：需求进来 → AI 辅助拆解 → AI 辅助开发 → AI 自动生成测试 → AI 预检 Review → 人工终审 → CI/CD 发布。

整个流程人做决策，AI 做执行。我的理解是 Vibecoding 不是让 AI 替你写代码，
而是把重复性的工程动作封装成可复用的智能体，让你专注于架构决策和代码质量。
```

### 5.3 常见追问速答

| 追问 | 一句话核心 |
|------|-----------|
| 为什么用 Taro 而不是 uniapp？ | Taro 的编译时方案对 React 技术栈更友好，我们的技术栈就是 React |
| 抖音迁移最大挑战？ | Taro API 在抖音运行时的兼容性差异 + 动画性能退化，需要逐个排查替代方案 |
| Monorepo 怎么管理依赖？ | Lerna + Yarn Workspace，公共依赖提升到根目录，组件独立发包 |
| 怎么保证 AI 代码质量？ | AI 生成 → Unit Test Skill 自动测试 → Code Review Skill 预检 → 人工终审 |
| 最近学了什么？ | MCP 协议深度研究 + Agent Skill 设计模式 |

---

## 六、优先级矩阵

按重要程度排序，时间不够时优先做前面的：

| 优先级 | 任务 | 理由 |
|--------|------|------|
| 🔴 P0 | AI 辅助开发经验话术 | 面试明确提到，是你的差异化亮点 |
| 🔴 P0 | 并发请求控制器手写 | 极高概率考，必须能手写 |
| 🔴 P0 | 项目 STAR 口述稿 | 每轮面试都用得上 |
| 🟡 P1 | React 原理深度 | 技术面必定深挖 |
| 🟡 P1 | EventEmitter 手写 | 高频手写题 |
| 🟡 P1 | 事件循环输出题 | 高频手写题 |
| 🟡 P1 | useDebounceFn / useQuery | 面试要点明确列出 |
| 🟢 P2 | 软素质话术 | 有准备就行 |
| 🟢 P2 | 闭包与作用域 | 基础题，应该已经会了 |

---

## 七、面试当天 Checklist

- [ ] 提前测试网络和设备
- [ ] 准备好纸笔（手写题可能需要）
- [ ] 打开准备好的手写题代码文件，可以快速扫一眼
- [ ] 复习一遍 3 分钟自我介绍
- [ ] 复习一遍 AI 经验话术
- [ ] 心态：字节OD面试重实战，展示你真正做过的事，不要背答案
