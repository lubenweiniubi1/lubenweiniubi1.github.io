# 项目实战与手写代码面试题

> 对应考察维度：**岗位相关** —— React 真实项目深度、中后台与 AI 工具实践、编码落地贴合度
> 同时覆盖：**基础素质** —— 沟通表达、技术热情、协作能力

---

## 一、React 真实项目深度

### Q1: "介绍一下你做过的最有挑战的 React 项目"

**AI 面试官考察点：**
- 项目规模与你的角色（是一个人扛还是螺丝钉）
- 技术难点是什么，你怎么解决的
- 有没有量化成果

**建议回答框架（STAR 法则 + 技术深度）：**

```
S (背景): "我在 XX 项目中负责前端架构，项目是一个中后台管理系统，
         日活 2000+，涉及 15 个业务模块"

T (任务): "需要解决两个核心问题：
         1. 大表单（100+ 字段）的性能问题
         2. 不同模块间 Table 组件的代码重复"

A (动作): "我做了三个层面的优化：
         1. 对 Table 组件做二次封装，配置化驱动，减少 60% 模板代码
         2. 大表单使用虚拟化 + 分步加载，将首屏渲染时间从 3s 降到 0.8s
         3. 引入 AI Coding 工具辅助生成样板代码，提效约 30%"

R (成果): "项目提前 2 周上线，代码复用率从 20% 提升到 65%"
```

### Q2: "你在项目中如何做技术选型？"

**回答思路：**
```
1. 约束条件（团队技术栈、工期、性能要求）
2. 方案对比（2-3 个候选方案的优劣势）
3. 选择依据（为什么选 A 而不是 B）
4. 落地验证（POC / 灰度验证）
```

**追问举例**："为什么选 react-query 而不是 SWR？"
- react-query：功能更全（mutation、infinite query、devtools）
- SWR：更轻量、API 更简单
- 本项目中后台需要复杂的 mutation 场景（乐观更新、失败回滚）→ react-query

---

## 二、中后台与 AI 工具实践

### Q3: 中后台通用 Table 组件的封装思路

**这是本次面试的重点考察方向！**

**设计思路（三层架构）：**

```
核心层（BaseTable）
├── 封装 Ant Design / Element Plus 的 Table
├── 统一分页、排序、筛选接口
├── 提供 loading / empty / error 三态
└── 暴露 rowKey / selectedRows 等通用能力

业务层（ProTable）
├── 配置化列定义（columns config → 自动渲染）
├── 工具栏（导出、刷新、列显隐、密度切换）
├── 搜索区域（表单 + Table 联动）
├── 批量操作
└── 行内编辑

样式层
├── 统一主题变量
└── 响应式适配
```

**配置化设计示例：**

```tsx
// 列的配置化定义
interface ColumnConfig<T> {
  key: string;
  title: string;
  dataIndex: string;
  width?: number;
  fixed?: 'left' | 'right';
  sortable?: boolean;
  filterable?: boolean;
  render?: (text: any, record: T, index: number) => React.ReactNode;
  hideInTable?: boolean;
  hideInSearch?: boolean;
  valueType?: 'text' | 'select' | 'date' | 'dateRange' | 'number';
  // 表单校验规则
  formItemProps?: Record<string, any>;
}

// 使用示例
const columns: ColumnConfig<User>[] = [
  { key: 'name', title: '姓名', dataIndex: 'name', sortable: true, width: 120 },
  { key: 'status', title: '状态', dataIndex: 'status', valueType: 'select',
    render: (val) => <Badge status={statusMap[val].color} text={statusMap[val].label} /> },
  { key: 'createdAt', title: '创建时间', dataIndex: 'createdAt', valueType: 'date' },
];
```

**Hook 封装（数据与视图解耦）：**

```tsx
function useTable<T>(api: (params: any) => Promise<{ data: T[]; total: number }>) {
  const [data, setData] = useState<T[]>([]);
  const [loading, setLoading] = useState(false);
  const [pagination, setPagination] = useState({ page: 1, pageSize: 20, total: 0 });
  const [filters, setFilters] = useState<Record<string, any>>({});
  const [sorter, setSorter] = useState<{ field: string; order: 'asc' | 'desc' } | null>(null);

  const fetchData = useCallback(async () => {
    setLoading(true);
    try {
      const params = { ...filters, ...sorter, ...pagination };
      const res = await api(params);
      setData(res.data);
      setPagination(prev => ({ ...prev, total: res.total }));
    } finally {
      setLoading(false);
    }
  }, [api, pagination.page, pagination.pageSize, filters, sorter]);

  // 防抖搜索
  const debouncedSearch = useMemo(
    () => debounce((values: any) => {
      setFilters(values);
      setPagination(prev => ({ ...prev, page: 1 }));
    }, 300),
    []
  );

  return { data, loading, pagination, setPagination, filters, setFilters: debouncedSearch,
           sorter, setSorter, refresh: fetchData };
}
```

### Q4: 你使用 AI Coding 工具的经验？

**字节 OD 面试高频新题！回答要有层次：**

**日常使用层面：**
- 代码补全：用 GitHub Copilot / Cursor 加速样板代码编写
- 代码生成：用 AI 生成单元测试、TypeScript 类型定义
- 代码审查：AI 辅助发现潜在 bug、安全隐患
- 文档生成：自动生成组件文档、API 说明

**深度实践层面：**
- 将设计规范/代码规范文档喂给 AI，让它按规范生成代码
- 用 AI 做代码重构建议（如 class component → hooks）
- AI 辅助排查报错（粘贴报错信息快速定位）

**关键认知（面试官想听到的）：**
1. "AI 是提效工具，不是替代——我会审查 AI 生成的每一行代码"
2. "AI 擅长老生常谈的代码，不擅长需要业务理解的逻辑"
3. "我建立了项目级别的 AI 规则文件，让 AI 遵循团队的代码规范"

### Q5: 你在项目中如何用 AI 提效？

**可量化案例（准备 1-2 个）：**

```
例子 1：用 Copilot 生成 Model 的类型定义
  - 之前：手写 50 个接口的 TypeScript 类型 → 2 天
  - 现在：复制后端 Swagger JSON → AI 生成 → 人工校验 → 半天
  - 提效：75%

例子 2：用 AI 生成单元测试
  - 之前：手写覆盖主要分支的测试 → 每个组件 30 分钟
  - 现在：提供组件代码 + 用例描述 → AI 生成 → 调整 → 10 分钟
  - 提效：66%
```

---

## 三、编码落地——手写代码题

### Q6: 带并发限制的 Promise 调度器（字节高频！）

```js
class PromiseScheduler {
  constructor(limit = 2) {
    this.limit = limit;
    this.running = 0;
    this.queue = [];
  }

  add(task) {
    return new Promise((resolve, reject) => {
      this.queue.push({ task, resolve, reject });
      this.run();
    });
  }

  run() {
    while (this.running < this.limit && this.queue.length > 0) {
      const { task, resolve, reject } = this.queue.shift();
      this.running++;
      task()
        .then(resolve, reject)
        .finally(() => {
          this.running--;
          this.run();
        });
    }
  }
}

// 使用
const scheduler = new PromiseScheduler(2);
const tasks = urls.map(url => () => fetch(url).then(r => r.json()));
Promise.all(tasks.map(t => scheduler.add(t))).then(results => console.log(results));
```

### Q7: 深拷贝（处理循环引用、Map、Set、Date、RegExp）

```js
function deepClone(obj, map = new WeakMap()) {
  // null / 基本类型
  if (obj === null || typeof obj !== 'object') return obj;

  // 循环引用
  if (map.has(obj)) return map.get(obj);

  // 特殊类型
  if (obj instanceof Date) return new Date(obj);
  if (obj instanceof RegExp) return new RegExp(obj.source, obj.flags);
  if (obj instanceof Map) {
    const clone = new Map();
    map.set(obj, clone);
    obj.forEach((v, k) => clone.set(deepClone(k, map), deepClone(v, map)));
    return clone;
  }
  if (obj instanceof Set) {
    const clone = new Set();
    map.set(obj, clone);
    obj.forEach(v => clone.add(deepClone(v, map)));
    return clone;
  }

  // 数组 / 普通对象
  const clone = Array.isArray(obj) ? [] : Object.create(Object.getPrototypeOf(obj));
  map.set(obj, clone);
  Reflect.ownKeys(obj).forEach(key => {
    clone[key] = deepClone(obj[key], map);
  });
  return clone;
}
```

### Q8: 一维数组转树形结构

```js
function arrayToTree(list, parentId = null) {
  // Hash 映射法 O(n)
  const map = {};
  const result = [];

  list.forEach(item => {
    item.children = [];
    map[item.id] = item;
  });

  list.forEach(item => {
    const pid = item.parentId;
    if (pid === parentId || !map[pid]) {
      result.push(item);
    } else {
      map[pid].children.push(item);
    }
  });

  return result;
}

// 递归版
function arrayToTree_recursive(list, parentId = null) {
  return list
    .filter(item => item.parentId === parentId)
    .map(item => ({
      ...item,
      children: arrayToTree_recursive(list, item.id),
    }));
}
```

### Q9: 实现 Promise.all / Promise.allSettled

```js
function promiseAll(promises) {
  return new Promise((resolve, reject) => {
    if (!promises.length) return resolve([]);
    const results = [];
    let count = 0;
    promises.forEach((p, i) => {
      Promise.resolve(p).then(
        val => {
          results[i] = val;
          count++;
          if (count === promises.length) resolve(results);
        },
        err => reject(err)  // 任一失败立即 reject
      );
    });
  });
}

function promiseAllSettled(promises) {
  return new Promise(resolve => {
    const results = [];
    let count = 0;
    promises.forEach((p, i) => {
      Promise.resolve(p).then(
        val => { results[i] = { status: 'fulfilled', value: val }; count++; if (count === promises.length) resolve(results); },
        err => { results[i] = { status: 'rejected', reason: err }; count++; if (count === promises.length) resolve(results); }
      );
    });
  });
}
```

### Q10: LRU 缓存（O(1) get + O(1) put，字节高频）

```js
class LRUCache {
  constructor(capacity) {
    this.capacity = capacity;
    this.map = new Map();  // Map 保持插入顺序
  }

  get(key) {
    if (!this.map.has(key)) return -1;
    const val = this.map.get(key);
    this.map.delete(key);
    this.map.set(key, val);  // 移至末尾（最新）
    return val;
  }

  put(key, value) {
    if (this.map.has(key)) this.map.delete(key);
    this.map.set(key, value);
    if (this.map.size > this.capacity) {
      // 删除最久未使用（Map 的第一个 key）
      this.map.delete(this.map.keys().next().value);
    }
  }
}
```

---

## 四、沟通表达与软素质（基础素质维度）

### AI 面试官评估软素质的方式

**AI 面试官会通过以下方式评估你的软素质：**

| 考察点 | AI 如何评估 | 你的策略 |
|--------|------------|----------|
| 逻辑连贯性 | 回答是否有清晰的结构（总-分-总） | 每个回答开头先给结论，再展开 |
| 是否有停顿/口头禅 | 语音识别分析停顿频率 | 遇到难题时用 "让我想一下" 替代 "额..." |
| 技术热情 | 是否主动提及新技术 | 每个话题都带一点最新动态（React 19、Vite 6、RSC） |
| AI 工具关注 | 是否用过 AI Coding 工具 | 准备好了——在 Q4/Q5 中主动展示 |
| 协作能力 | 项目描述中是否有"我们" | "我和后端讨论后决定..."、"团队 Code Review 时..." |

### 面试中避免的红灯词

```
❌ "这个我不太清楚"       → ✅ "这方面我的经验有限，但我了解的原理是..."
❌ "就是那样写的"         → ✅ "我选择这个方案是因为..."
❌ "是领导/同事决定的"    → ✅ "我们团队评估后选择了...因为..."
❌ "额...嗯..."（高频）   → ✅ 短暂停顿 + 喝口水
```

---

## 五、高频算法题速查

| 题目 | 考点 | 字节出现频率 |
|------|------|-------------|
| 反转链表 | 链表操作 | ⭐⭐⭐⭐⭐ |
| 大数相加 | 字符串处理 | ⭐⭐⭐⭐ |
| 有效的括号 | 栈 | ⭐⭐⭐⭐ |
| 最长回文子串 | 动态规划/中心扩展 | ⭐⭐⭐ |
| 合并两个有序数组 | 双指针 | ⭐⭐⭐ |
| 实现 Promise.all | 异步控制 | ⭐⭐⭐⭐⭐ |
| LRU 缓存 | Hash+双向链表 | ⭐⭐⭐⭐⭐ |
| 数组 flat | 递归/迭代 | ⭐⭐⭐ |
| 深拷贝 | 递归+WeakMap | ⭐⭐⭐⭐ |

---

## 六、总结：面试前的最后检查清单

```
□ React Fiber 原理能讲清楚（DFS 遍历 + 可中断 + 双缓冲）
□ Hooks 闭包陷阱能写出示例 + 解决方案
□ useMemo / useCallback 能讲清楚何时用何时不用
□ 虚拟列表原理能画图说明
□ 防抖节流能手写
□ Table 二次封装能讲出三层架构
□ AI Coding 使用经验能举出量化案例
□ Promise 调度器 + LRU Cache 能手写
□ 准备 1 个能展示技术深度的项目案例
□ ESList/Prettier/Husky 工程化流程能串起来
```
